---
phase: 00-fix-to-run-ci-net
plan: 04
type: execute
wave: 3
depends_on: ["00-01", "00-02", "00-03"]
files_modified:
  - scripts/check-model-allowlist.mjs
  - .github/workflows/ci.yml
  - package.json
  - .env.local.example
  - index.html
  - metadata.json
  - README.md
autonomous: true
requirements: [FIX-01, FIX-05, FIX-06]
must_haves:
  truths:
    - "CI runs automatically on every push and pull request to the existing GitHub remote and runs tsc + the full Vitest suite (D-11: Vitest + mocked SDK + test script) + the model-id allowlist check"
    - "A reintroduced bad/unknown model id fails the CI allowlist check without spending any Gemini quota (no GEMINI_API_KEY in CI)"
    - "The allowlist check asserts the MODEL_ID used by the server is a member of ALLOWLIST"
    - ".env.local.example exists documenting GEMINI_API_KEY (and optional PORT)"
    - "AI Studio template metadata is replaced with the real app identity in index.html, metadata.json, package.json name, and README"
  artifacts:
    - "scripts/check-model-allowlist.mjs — static allowlist assertion"
    - ".github/workflows/ci.yml — Bun install + tsc + vitest + allowlist on push/PR"
    - "package.json with a test script and a real name"
    - ".env.local.example"
    - "cleaned index.html / metadata.json / README.md"
  key_links:
    - "ci.yml → bun install --frozen-lockfile → bunx tsc --noEmit → bunx vitest run → bunx scripts/check-model-allowlist.mjs"
    - "check-model-allowlist.mjs imports MODEL_ID + ALLOWLIST from src/schema/modelAllowlist.ts and exits non-zero if MODEL_ID ∉ ALLOWLIST"
---

<objective>
Lock the fixes behind an automated CI net and remove the AI Studio template scaffolding. Add a static model-id allowlist check (no live API, no quota), a GitHub Actions workflow that installs with Bun and runs tsc + the full Vitest suite + the allowlist check on every push/PR to the existing remote, add the `test` script, create `.env.local.example`, and replace template metadata with the real app identity.

Purpose: Success Criterion 4 (CI runs automatically: allowlist + happy-path + malformed tests, so a reintroduced bad id or unguarded parse fails the build) plus the CI-side of Success Criterion 1 and the cleanup half of Success Criterion 3. This is the final slice — it makes the whole phase's correctness enforceable and cleans up onboarding.
Output: `check-model-allowlist.mjs`, `ci.yml`, `.env.local.example`, cleaned template files, `test` script.

D-09 note: git is ALREADY initialized with a GitHub remote (`origin git@github.com:anhntcn/affipro.git`, verified). This plan does NOT run `git init` or create the repo — it only adds the CI workflow and ensures it runs on push/PR to the existing remote.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-fix-to-run-ci-net/00-RESEARCH.md
@package.json
@index.html
@metadata.json
@README.md
@src/schema/modelAllowlist.ts
@.claude/CLAUDE.md
</context>

## Phase Goal

**As a** developer on this team, **I want to** have CI automatically reject a bad model id or an unguarded-parse regression on every push, and have a clean, accurately-documented project to onboard into, **so that** the Phase 0 fixes cannot silently regress when later phases rewrite server.ts.

(CI-net + cleanup slice — the enforcement layer for the whole phase.)

## Artifacts this plan produces

| Symbol / Path | Kind | Notes |
|---------------|------|-------|
| `scripts/check-model-allowlist.mjs` | ES module (run via `bunx`) | asserts MODEL_ID ∈ ALLOWLIST |
| `.github/workflows/ci.yml` | CI workflow | Bun install + tsc + vitest + allowlist, on push/PR |
| `package.json` "test" + real name | scripts/metadata | `"test": "vitest run"` |
| `.env.local.example` | env template | GEMINI_API_KEY, PORT |
| cleaned `index.html` / `metadata.json` / `README.md` | metadata cleanup | real app identity, Bun instructions |

<tasks>

<task type="auto">
  <name>Task 1: Static model-id allowlist check + package.json test script</name>
  <read_first>src/schema/modelAllowlist.ts, package.json, .planning/phases/00-fix-to-run-ci-net/00-RESEARCH.md (§Gemini Model ID, D-10)</read_first>
  <files>scripts/check-model-allowlist.mjs, package.json</files>
  <action>
Create `scripts/check-model-allowlist.mjs` (an ES module — the repo is `"type": "module"`) that imports `MODEL_ID` and `ALLOWLIST` from `../src/schema/modelAllowlist.ts`. This script is ALWAYS invoked via `bunx` (never plain `node`) because Bun resolves the `.ts` import directly — plain `node` cannot import a `.ts` file, so a `node scripts/check-model-allowlist.mjs` invocation would throw before the allowlist assertion runs and the FIX-01 regression check would never execute. Commit to ONE invocation form — `bunx scripts/check-model-allowlist.mjs` — used identically here, in the `package.json` script, and in the CI workflow (Task 2). Assert `ALLOWLIST.includes(MODEL_ID)`; on success print a confirmation and exit 0; on failure print which id was used and that it is not in the allowlist, then `process.exit(1)`. This runs with NO `GEMINI_API_KEY` and makes no network call (D-10 — static check, no quota).

In `package.json`: add `"test": "vitest run"` to scripts, and add a convenience `"check:model": "bunx scripts/check-model-allowlist.mjs"` (must match the CI invocation in Task 2 exactly — `bunx`, not `node`). Keep the existing scripts intact.
  </action>
  <verify>
    <automated>bunx scripts/check-model-allowlist.mjs && grep -q '"test": "vitest run"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `bunx scripts/check-model-allowlist.mjs` exits 0 while `MODEL_ID` is in `ALLOWLIST`.
    - Temporarily setting MODEL_ID to an id not in ALLOWLIST makes the script exit non-zero (spot-check; revert after).
    - `package.json` has `"test": "vitest run"`.
    - The script makes no network call and needs no `GEMINI_API_KEY`.
  </acceptance_criteria>
  <done>A static allowlist check asserts MODEL_ID ∈ ALLOWLIST with no quota spend, and the test script is wired into package.json.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions CI workflow (Bun install + tsc + vitest + allowlist)</name>
  <read_first>package.json, scripts/check-model-allowlist.mjs, .planning/phases/00-fix-to-run-ci-net/00-RESEARCH.md (§GitHub Actions CI, §Validation Architecture, D-09/D-10/D-11)</read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` that triggers `on: [push, pull_request]` (to the existing `origin` remote — do not create the repo, it exists). One job `ci` on `ubuntu-latest` that: checks out the repo (actions/checkout), sets up Bun (oven-sh/setup-bun), installs with `bun install --frozen-lockfile` (Bun is the standardized package manager — bun.lock is the committed lockfile), then runs three steps: `bunx tsc --noEmit`, `bunx vitest run`, and `bunx scripts/check-model-allowlist.mjs`. The allowlist step MUST use `bunx` (NOT `node`): the script imports `../src/schema/modelAllowlist.ts`, and plain `node` cannot import a `.ts` file — under `node` the step would throw before asserting the allowlist, so the core FIX-01 regression guard would silently never run in CI. This `bunx scripts/check-model-allowlist.mjs` invocation must match Task 1's `package.json` `check:model` script byte-for-byte. Do NOT add a `GEMINI_API_KEY` secret to the workflow (D-10 — the SDK is mocked in tests; the allowlist check is static). Ensure `NODE_ENV` is not forced to production in a way that would require the key. Keep the workflow minimal (no coverage gate required, no deploy).
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'frozen-lockfile' .github/workflows/ci.yml && grep -q 'vitest run' .github/workflows/ci.yml && grep -q 'check-model-allowlist' .github/workflows/ci.yml && ! grep -q 'GEMINI_API_KEY' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists, triggers on push and pull_request, installs via `bun install --frozen-lockfile`, and runs `bunx tsc --noEmit`, `bunx vitest run`, and `bunx scripts/check-model-allowlist.mjs` (the allowlist step uses `bunx`, not `node`, and matches the `package.json` `check:model` script exactly).
    - The workflow contains no `GEMINI_API_KEY` secret reference.
    - Locally, the three commands the workflow runs all pass (`bunx tsc --noEmit`, `bunx vitest run`, the allowlist script).
  </acceptance_criteria>
  <done>CI runs tsc + full Vitest + the static allowlist check on every push/PR to the existing remote, with no Gemini key/secret.</done>
</task>

<task type="auto">
  <name>Task 3: .env.local.example + strip AI Studio template metadata</name>
  <read_first>index.html, metadata.json, README.md, package.json, .env.example, .planning/codebase/CONCERNS.md (§Tech Debt — template scaffolding), .planning/phases/00-fix-to-run-ci-net/00-CONTEXT.md (D-08)</read_first>
  <files>.env.local.example, index.html, metadata.json, package.json, README.md</files>
  <action>
Create `.env.local.example` documenting the required `GEMINI_API_KEY` (with a placeholder value and a one-line comment that it is server-side only) and the optional `PORT` (default 3000). This is the file the README should reference (the current README points at a nonexistent `.env.local`).

Clean the AI Studio template metadata (D-08 / CONCERNS §Tech Debt): in `index.html` replace the title and all description/OG/Twitter meta ("My Google AI Studio App" / "An application built with Google AI Studio.") with the real app name "Affiliate Content Pro" and a real Vietnamese/English description of the multi-channel affiliate content generator. In `metadata.json` set the real `name` and `description` (keep `majorCapabilities`). In `package.json` change `"name": "react-example"` to a real slug (e.g. `affiliate-content-pro` or `affipro`). Rewrite `README.md` for the actual project: correct the run instructions to Bun (`bun install`, `bun run dev`), reference `.env.local.example` → copy to `.env.local` and set `GEMINI_API_KEY`, and remove the AI Studio banner/URL. Do not touch the `.gitignore` rules that keep `.env*` (except the example) out of git.
  </action>
  <verify>
    <automated>test -f .env.local.example && grep -q 'GEMINI_API_KEY' .env.local.example && ! grep -q 'My Google AI Studio App' index.html && ! grep -q 'An application built with Google AI Studio' index.html && ! grep -q 'react-example' package.json && grep -qi 'bun install' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local.example` exists and documents `GEMINI_API_KEY` (and PORT).
    - `index.html` no longer contains "My Google AI Studio App" or "An application built with Google AI Studio."; it shows the real app identity.
    - `metadata.json` has a non-empty real `name`/`description`; `package.json` name is no longer `react-example`.
    - `README.md` uses Bun run instructions and references `.env.local.example`, not a nonexistent `.env.local` alone.
  </acceptance_criteria>
  <done>.env.local.example documents the required key; all AI Studio template metadata is replaced with the real Affipro identity and Bun-based instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source repo → CI | CI is the enforcement boundary that blocks a regressed model id / unguarded parse before it merges |
| repo docs/metadata → developer onboarding | Template metadata misrepresents the app and points at a nonexistent env file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-11 | Tampering | A future edit reintroduces a bad/unknown/shut-down model id | high | mitigate | Static allowlist check in CI asserts MODEL_ID ∈ ALLOWLIST on every push/PR (FIX-01, D-10) — fails the build, no quota spent |
| T-00-12 | Tampering | An unguarded-parse / crash regression slips into server.ts | high | mitigate | Full Vitest suite (happy + malformed + safety + config) runs in CI on every push/PR (FIX-06) |
| T-00-13 | Info Disclosure | Adding a `GEMINI_API_KEY` secret to CI (unnecessary quota/leak surface) | medium | mitigate | Workflow explicitly carries NO Gemini secret; tests mock the SDK, allowlist is static (D-10) |
| T-00-14 | Info Disclosure | Stale template metadata / README pointing at a nonexistent env file misleads onboarding | low | mitigate | Replace template metadata with real identity; `.env.local.example` documents the real key (D-08) |
| T-00-SC | Tampering | supply-chain — CI uses pinned action versions + frozen lockfile | high | mitigate | `bun install --frozen-lockfile` (no drift from committed bun.lock); use tagged action versions for checkout/setup-bun |

*Note: `0.0.0.0` bind + rate limiting remain ACCEPTED/DEFERRED to public-ready (PUB-0x) per RESEARCH §Security Domain — out of Phase 0 scope; documented-accepted so the block_on:high gate treats them as closed.*
</threat_model>

<verification>
- `bunx scripts/check-model-allowlist.mjs` exits 0.
- `.github/workflows/ci.yml` triggers on push/PR, installs with `bun install --frozen-lockfile`, runs tsc + vitest + allowlist, and has no Gemini secret.
- `bunx tsc --noEmit` and `bunx vitest run` pass locally (the exact commands CI runs).
- `.env.local.example` exists; no AI Studio template strings remain in index.html/metadata.json/package.json/README.
</verification>

<success_criteria>
- CI runs automatically on every change and fails the build on a reintroduced bad model id or an unguarded-parse regression.
- The model-id check is static (no quota, no key in CI).
- `.env.local.example` exists and the AI Studio template metadata is fully replaced.
</success_criteria>

<output>
Create `.planning/phases/00-fix-to-run-ci-net/00-04-SUMMARY.md` when done.
</output>
