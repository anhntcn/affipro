---
phase: 00-fix-to-run-ci-net
plan: 03
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - src/components/ErrorBoundary.tsx
  - src/components/ResultDisplay.tsx
  - src/App.tsx
  - tests/ResultDisplay.test.tsx
autonomous: true
requirements: [FIX-04]
must_haves:
  truths:
    - "When the generated content is missing a field or an array (e.g. no key_benefits, no scenes, no hashtags), ResultDisplay renders without throwing a React error — the user never sees a white screen"
    - "A render-time error anywhere inside ResultDisplay is contained by an ErrorBoundary that shows a Vietnamese fallback message instead of blanking the app"
    - "Every .map()/array access and .join() in ResultDisplay is null-guarded"
  artifacts:
    - "src/components/ErrorBoundary.tsx — a React class component with getDerivedStateFromError + a Vietnamese fallback"
    - "src/components/ResultDisplay.tsx — null-guarded array access on all tabs"
    - "src/App.tsx — ResultDisplay wrapped in ErrorBoundary"
    - "tests/ResultDisplay.test.tsx — render-with-missing-fields test (jsdom)"
  key_links:
    - "App.tsx → <ErrorBoundary><ResultDisplay/></ErrorBoundary> → each tab guards (x ?? []).map / (x ?? []).join"
---

<objective>
Make the result UI crash-proof against missing fields. Add a class-based ErrorBoundary (React 19 has no hook equivalent) with a Vietnamese fallback, wrap ResultDisplay in it, and null-guard every array access / .map() / .join() in ResultDisplay so a payload missing any field renders gracefully instead of white-screening. Prove it with a jsdom render test that mounts ResultDisplay with a missing-field payload.

Purpose: Success Criterion 2 (client-side half — ErrorBoundary + null-guards so the user never sees a white screen). This is the client-resilience slice; it is independent of the server slice (Plan 02) and runs in parallel.
Output: `ErrorBoundary`, guarded `ResultDisplay`, and a passing render test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-fix-to-run-ci-net/00-RESEARCH.md
@src/components/ResultDisplay.tsx
@src/App.tsx
@src/store/useStore.ts
@src/types.ts
</context>

## Phase Goal

**As an** affiliate marketer, **I want to** still see a readable screen (and a clear Vietnamese notice) even when the AI result is missing a section, **so that** one bad field never blanks the whole app while I'm working.

(Client-resilience slice of the Phase 0 story; independent of the server slice.)

## Artifacts this plan produces

| Symbol / Path | Kind | Notes |
|---------------|------|-------|
| `ErrorBoundary` (`src/components/ErrorBoundary.tsx`) | React class component | Vietnamese fallback |
| guarded `ResultDisplay` (`src/components/ResultDisplay.tsx`) | component edit | `(x ?? [])` on all arrays |
| `App.tsx` wrap | component edit | ErrorBoundary around ResultDisplay |
| `tests/ResultDisplay.test.tsx` | test (jsdom) | render-with-missing-fields |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ErrorBoundary class component with Vietnamese fallback</name>
  <read_first>src/App.tsx, src/components/ResultDisplay.tsx, .planning/phases/00-fix-to-run-ci-net/00-RESEARCH.md (§Pattern 4)</read_first>
  <files>src/components/ErrorBoundary.tsx, src/App.tsx</files>
  <behavior>
    - A child that throws during render, wrapped in ErrorBoundary → the fallback Vietnamese message is shown, the app does not blank.
    - A child that renders normally → children are shown unchanged.
  </behavior>
  <action>
Create `src/components/ErrorBoundary.tsx` per Pattern 4: a `class ErrorBoundary extends Component<{ children: ReactNode }, { hasError: boolean }>` with `state = { hasError: false }`, a static `getDerivedStateFromError()` returning `{ hasError: true }`, a `componentDidCatch(err)` that logs via `console.error`, and a `render()` that — when `hasError` — returns a styled red fallback block with a Vietnamese message telling the user the result could not be displayed and to try generating again (consistent tone with the existing red banner in App.tsx), otherwise returns `this.props.children`. Must be a class component (React 19 has no hook form of error boundaries).

In `src/App.tsx`, import `ErrorBoundary` and wrap the `<ResultDisplay />` render (the right-column branch that renders when `generatedContent` is truthy) in `<ErrorBoundary>...</ErrorBoundary>`. Do not change the empty-state branch or the existing error banner.
  </action>
  <verify>
    <automated>bunx tsc --noEmit && bunx vitest run tests/ResultDisplay.test.tsx -t "error boundary"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ErrorBoundary.tsx` exports a class component with `getDerivedStateFromError` and a Vietnamese fallback string.
    - `src/App.tsx` wraps `<ResultDisplay />` in `<ErrorBoundary>`.
    - `bunx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>A class ErrorBoundary with a Vietnamese fallback wraps ResultDisplay in App.tsx and contains render-time errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Null-guard every array access in ResultDisplay</name>
  <read_first>src/components/ResultDisplay.tsx, src/types.ts, .planning/codebase/CONCERNS.md (§Known Bugs — UI .map), .planning/phases/00-fix-to-run-ci-net/00-RESEARCH.md (§Pattern 4 null-guard list)</read_first>
  <files>src/components/ResultDisplay.tsx</files>
  <behavior>
    - AnalysisTab with product_analysis.key_benefits undefined → renders (empty list), no throw.
    - FacebookTab with product_highlights undefined and hashtags undefined → renders and the copy string builds, no throw.
    - VideoTab with short_video_script.scenes undefined → renders (no scenes), no throw.
  </behavior>
  <action>
In `src/components/ResultDisplay.tsx`, wrap every array access and iteration in a default-array guard so missing fields cannot throw (defense in depth even with server-side Zod). Specifically: `key_benefits` (AnalysisTab `.map`), `product_highlights` (FacebookTab `.map` and the `.map(...).join('\n')` inside `contentString`), `hashtags` (the `.join(' ')` in both `contentString` and the rendered line), and `scenes` (VideoTab `.map`) — change each to guard with `(value ?? [])` before `.map`/`.join`. Do not change styling, tab structure, copy behavior, or the snake_case field names. The nested string fields (e.g. `data.product_name`) may remain as-is since the ErrorBoundary contains any residual throw and the server guarantees shape; the required change is on the array operations that CONCERNS flagged (the `.map`/`.join` sites).
  </action>
  <verify>
    <automated>bunx tsc --noEmit && bunx vitest run tests/ResultDisplay.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Every `.map(` and `.join(` on `key_benefits`, `product_highlights`, `hashtags`, `scenes` is preceded by a `?? []` (or equivalent) guard.
    - `bunx tsc --noEmit` passes.
    - The render tests in Task 3 pass with missing-field payloads.
  </acceptance_criteria>
  <done>All array operations in ResultDisplay are null-guarded; a missing array field renders empty instead of throwing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render test — ResultDisplay with a missing-field payload (jsdom)</name>
  <read_first>src/components/ResultDisplay.tsx, src/store/useStore.ts, src/components/ErrorBoundary.tsx, .planning/phases/00-fix-to-run-ci-net/00-RESEARCH.md (§Validation Architecture — render test)</read_first>
  <files>tests/ResultDisplay.test.tsx</files>
  <behavior>
    - Render ResultDisplay with a store payload missing key_benefits/product_highlights/hashtags/scenes → no thrown error, the component mounts.
    - Render a child that throws inside ErrorBoundary → the Vietnamese fallback text appears.
  </behavior>
  <action>
Create `tests/ResultDisplay.test.tsx` using `@testing-library/react` + jsdom (the vitest config from Plan 01 maps `*.test.tsx` to the jsdom environment). Because `ResultDisplay` reads from the Zustand store (`useStore`), set the store state before rendering: import `useStore` and call `useStore.setState({ generatedContent: <missing-field payload>, error: null })`, where the payload is a `GeneratedContent`-shaped object with several arrays deliberately absent (omit `key_benefits`, `product_highlights`, `hashtags`, and `scenes`; keep the string fields present so tab switching is exercisable). Render `<ResultDisplay />` inside the real `<ErrorBoundary>` and assert it mounts without throwing (e.g. the "Kết Quả Nội Dung" heading is present). Add a small always-throwing test child rendered inside `<ErrorBoundary>` to assert the Vietnamese fallback text renders (the "error boundary" -t case from Task 1). Reset the store in afterEach.
  </action>
  <verify>
    <automated>bunx vitest run tests/ResultDisplay.test.tsx && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run tests/ResultDisplay.test.tsx` exits 0.
    - The missing-field render test mounts ResultDisplay without a thrown error.
    - The ErrorBoundary test asserts the Vietnamese fallback text appears when a child throws.
  </acceptance_criteria>
  <done>A jsdom render test proves ResultDisplay survives missing fields and the ErrorBoundary shows its Vietnamese fallback on a thrown child.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSON → React render | The client renders a server payload; a missing/renamed field must not crash the render even though the server now validates (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-09 | DoS (client availability) | Missing-field payload throwing during render → white screen | high | mitigate | Null-guard every `.map`/`.join` in ResultDisplay + class ErrorBoundary fallback (FIX-04) |
| T-00-10 | Info Disclosure | ErrorBoundary fallback leaking a raw error/stack to the user | low | mitigate | Fallback shows a fixed Vietnamese message only; the caught error is logged via `console.error`, not rendered |
| T-00-SC | Tampering | supply-chain (no new installs in this plan) | high | accept | `@testing-library/react` + `jsdom` were installed and audited in Plan 01; no new installs here |

*Note: rate limiting / `0.0.0.0` bind remain ACCEPTED/DEFERRED to public-ready (PUB-0x) per RESEARCH §Security Domain — not in this client-only plan's scope.*
</threat_model>

<verification>
- `bunx tsc --noEmit` passes.
- `bunx vitest run tests/ResultDisplay.test.tsx` passes (missing-field render + ErrorBoundary fallback).
- Every array op in ResultDisplay is null-guarded; ResultDisplay is wrapped in ErrorBoundary in App.tsx.
</verification>

<success_criteria>
- A result payload missing any array field renders without a white screen.
- A render-time error is contained by the ErrorBoundary with a Vietnamese fallback.
</success_criteria>

<output>
Create `.planning/phases/00-fix-to-run-ci-net/00-03-SUMMARY.md` when done.
</output>
